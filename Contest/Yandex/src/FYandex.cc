#include<bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n;
	cin >> n;
	vector<int> p(n);
	if(n == 2) return cout << 1 << endl, 0;
	for(int i = 0; i < n; ++i) {
		int x,y;
		cin >> x >> y;
	}

}
